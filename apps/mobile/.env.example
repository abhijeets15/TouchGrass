# Copy this to .env and fill in your values

# Auth API (optional — auto-detected in Expo Go from your Metro host)
# Android emulator only if auto-detect fails: http://10.0.2.2:3001
# Physical device only if auto-detect fails: http://YOUR_COMPUTER_IP:3001
# EXPO_PUBLIC_API_URL=http://10.0.2.2:3001

# Itinerary generation (dev only — move behind apps/api before production)
# https://console.anthropic.com
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-your-key-here
